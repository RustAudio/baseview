[package]
name = "cursors"
version = "0.1.0"
edition = "2024"

[dependencies]
baseview = { path = "../..", features = ["opengl", "tracing"] }
femtovg = "0.26"
tracing-subscriber = { workspace = true }
